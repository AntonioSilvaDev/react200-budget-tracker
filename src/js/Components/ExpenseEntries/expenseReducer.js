const defaultState = {
    description: '',
    amount: '',
    lineItems: []
};

export default function ExpenseReducer (state = defaultState, action) {
    //`state = defaultState` is new es6 syntax
    //for defining default value on a param
    return state;
}